# 《急刹车》视觉圣经

## 1. 视觉命题

- 游戏与受众：面向移动端碎片时间玩家的单手反应游戏，首局无需阅读说明。
- 情绪承诺：看着断崖持续逼近，在唯一一次点击前屏息判断，并在自动制动后等待结果揭晓。
- 一句话方向：一支可收藏的低多边形极地远征队在断裂冰舌上轮流挑战急停，每个体型、动作、天气和镜头变化都服务于“它还能滑多远”。
- 标志性瞬间：镜头贴近并绕向断崖侧面，角色后仰、手脚撑开、雪屑喷散，最终停在有冰柱与浪花的崖口 8 px 内。
- 必须具备：角色轮廓和体型差异清楚、冰崖有可信厚度与地貌层次、距离易判断、收藏入口不打断核心操作。
- 避免方向：写实摄影、廉价通用卡通 UI、霓虹玻璃科技感。

## 2. 构图与镜头

- 方向与比例：竖屏固定游戏场 390×700，3D 场景填满画布，DOM HUD 内部适配；同时验证 320×568 与 390×844。
- 首屏镜头：使用以角色为中心、位于前侧约 35° 的 `(characterX+6.8, 6.8, 4.8)`、zoom 64 三分之四英雄机位，观察点锁定当前角色胸口；用 3.2 秒完成幅度不超过 0.35u 的轻微侧向漂移。画面需同时看见脸、胸口、左右肢体和朝向跑道的脚尖，角色高度约占游戏场 14–18%，不以背面轮廓或远景展示牺牲角色辨识度。
- 开始换位：每一关先在角色英雄近景停留 520 ms；重试后的等待状态不限时停在同一近景，并显示幽灵手指，直到玩家再次点击。角色真正开始滑动的同一帧，相机才用 1050 ms ease-out 拉远到以整条跑道中点为观察中心的 `(trackCenter-4.0, 14.0, 21.0)`、zoom 6.8 全景机位，并将晴朗/薄雾/风雪的远景雾距分别保持在 42/32/34u。画面必须同时看见起点、连续完整的三段长跑道、尽头悬崖、完整崖口和少量崖外海面，跑道不能被任一侧画框裁切。到位后相机固定停留 1000 ms，角色继续滑行且可随时刹车；停留结束才进入推进。
- 游戏镜头：全景停留结束后将 OrthographicCamera 连续推近到以角色为中心、位于角色前侧约 32° 的 `(characterX+5.8, 5.2, 3.6)`，zoom 6.8→68。急停瞬间优先移动到 `(characterX+6.2, 5.8, 3.2)`、zoom 至少 54，使脸、双臂和双脚同时可见。三种结果使用彼此独立的镜头语法：成功先定格 120 ms，再用 980 ms ease-out 从刹车机位绕到悬崖外侧的角色正前方 `(characterX+4.7, 4.25, -4.6)`、zoom 78；停得太远先定格 110 ms，再用 930 ms 升高拉远到剩余赛道中央的俯视 zoom 7.4 全景，把人物到悬崖的空白距离变成失败证据；坠崖先冻结 140 ms，随后保持 X 方向近似不变并沿崖壁垂直下坠，观察点从胸口降至 y=-1.35，zoom 84→62，不允许横向绕人或旋转到地面。
- 主要焦点：画面中部 y=280–470 的当前角色、冰面与悬崖边缘。
- 层次：前景风雪、雪屑与近岸碎冰；中景角色、裂缝冰舌与立体断崖；背景为浮冰、冰柱、低对比冰山、天气层，以及只在全景建立阶段浮现的随机巨物剪影。巨物完整模型统一放大到 28–34u 高，并位于跑道后方的深海雾带；模型约 60% 沉在海面与雾层下，只露出足以辨认类别的巨大头部、肩背、角或机械上盖，不得形成比跑道更亮的第二视觉焦点。
- 浮岛厚度：借用悬浮山体“断裂悬挑、根系垂落”的空间节奏，但几何语言严格保持《跳一跳》式方块。跑道的连续薄冰基座下交替出现两条延伸出画框的深长方体、两段画面内结束的中短方体和 6 个短方块；各块宽度、长度、前后深度和间隙不同，形成错落负空间，不做上宽下收。最后 5% 崖口必须保持薄而危险的悬挑。
- HUD 安全区：顶部 28–112 px；底部 566–670 px 为教学/按压提示区，不遮挡赛道。
- 注意路径：角色前进方向 → 明亮冰崖切口与垂直冰壁 → 崖外海面 → 点击反馈与距离结果。

## 3. 色彩

- 天空背景：极夜蓝 `#10263B`；远景冰山 `#1F435A`；雪面 `#EAF9F7`；冰侧 `#A9DCD8`。
- 主文字：暖雪白 `#F7FBF6`；次文字 `#A9C2C5`；墨色轮廓 `#102129`。
- 角色：完整沿用共享素材库 52 名角色的暖中性色、压灰服装色与统一青绿强调 `#3FB6AC`；程序化企鹅退出正式角色池。
- 操作强调：刹车珊瑚红 `#F06449`；贴边奖励黄 `#FFD166`；安全青绿 `#69D1B5`；失败红 `#E9504F`。
- 使用比例：天空与远景 55%，雪与冰面 25%，角色及轮廓 14%，强调色不超过 6%。
- 禁止：蓝底上用低对比蓝字；同时大面积使用珊瑚红和奖励黄；用颜色作为唯一评价信号。

## 4. 字体

- 展示与数字：`Arial Rounded MT Bold`, `Nunito`, `PingFang SC`, sans-serif；数字使用 tabular-nums。
- 正文与按钮：`Avenir Next`, `PingFang SC`, `Microsoft YaHei`, sans-serif。
- 标题 38/44 px、900；分数 30/34 px、800；按钮 18/22 px、800；辅助标签 13/18 px、700。
- 只允许分数与短标签全大写英文；中文不加宽字距，不使用描边文字。

## 5. 形状、材质与光线

- 主导形状：角色沿用素材库 box / low-segment cyl / icosa / cone / wedge 的体素语言；冰崖由错层盒体、楔形雪脊、六段冰柱和碎冰组成；UI 为 12–18 px 圆角的实心远征标签。
- 边缘：全部 3D 素材使用 flatShading 的硬切面，不加卡通描边；轮廓由三值光照与接触阴影建立。
- 材质：哑光低多边形材质，`metalness=0`、高粗糙度，不使用照片纹理或平滑高模表面。
- 光线：顶部主光、冷色填充光与暖色轮廓光形成 1.0/0.8/0.62 三值面；冰崖切面比顶面深 20–25%，角色和冰面必须有接触阴影。

## 6. 角色、环境与素材

- 角色阵容：完整接入素材库 52 名可扮演角色，覆盖 15 名普通人物、11 名文化职业、6 名怪物、5 名办公室角色、2 名精英角色、1 名机甲、1 名神话角色与 11 名动物。模型保持原始轮廓和三值光照，通过统一落脚点与按原始高度归一的整体缩放进入游戏；矮胖、宽脚和低重心角色对应较高抓地，瘦高、重甲或接地窄角色对应较低抓地。
- 朝向：全部人形及特殊人形的脸部位于本地 +Z，使用 Y=+90° 对齐跑道；全部动物头部位于本地 +X，使用 Y=0° 对齐跑道。首屏为了看脸只能移动相机，禁止把角色本体斜转向镜头。
- 姿态：待机以 0.04–0.10u 起伏和 2–5% 纵向拉伸表现呼吸。具名 `rig_legL/rig_legR/rig_armL/rig_armR` 节点的人形直接使用素材库枢轴，滑行周期为 0.72–0.95 秒，左右腿轮流后外侧蹬冰，双臂反向大幅摆动；刹车时双脚横撑、双臂向后抬并反相晃动。无腿幽灵改用悬浮波浪、前倾和刹车回弹；战斗机甲使用 0.9 倍步频、更小形变和更强落脚停顿；四足动物使用对角足组与身体俯仰；鸡、鸭使用双足短步与翅膀平衡；青蛙使用双后腿同步蹬伸和伏地急停。失败统一冻结 140 ms 后替换为 14 块按角色分类取色的低多边形碎片，0.35 倍速度散落 1250 ms，禁止完整身体侧翻、肢体穿过躯干或碎片瞬间消失。
- 环境：主冰舌至少包含 3 层厚度、2 条可见裂缝、3 组雪脊、5 根冰柱、4 块近岸碎冰和带浪脊的深海；顶面、侧壁、深层冰与无边界海域底色形成四级明度。场景不绘制水平海面网格，避免正交相机近端裁切产生被误读为跑道阴影的直线；海域由全画布底色、崖下浪脊、漂浮碎冰和天气雾共同表达。加长跑道按 0–30%、30–68%、68–100% 分成稳滑、加速、临崖三段，用低对比青蓝冰带和越来越密的短划纹提示速度变化，不使用箭头或文字路牌。远海巨物直接复用 11 个本地素材库 GLB，覆盖大型动物、怪兽、机甲与神话角色；保持原始 low-poly 轮廓，统一施加雾色剪影材质、28–34u 高度与约 60% 的海面下沉，并使用固定世界高度裁切平面彻底移除下部身体；截面依靠低透明度自然软化，不叠加实体雾片。
- 天气：晴朗保持高对比；飘雪使用 120 个慢速点状雪片；薄雾缩短远景雾距但保留角色与崖口；风雪使用 180 个快速斜移雪点和更冷的环境光。约 650 ms 淡入，天气层跟随镜头。
- 游戏资产使用共享 low-poly 库的模型与同源图元，几何吸附 0.125u 网格，统一 flat-shaded 三值语言；不混用照片、平滑 3D 高模、2D 贴片或 emoji。

## 7. UI 与图标

- 图标：自绘 24 px SVG 线性图标，2.2 px 圆头描边；声音、金币、角色和关闭使用同一套轮廓规则并提供可访问名称。
- 主按钮：最小 168×52 px，珊瑚红实心、墨色 2 px 下边阴影；按下下移 3 px并缩放至 0.98。
- HUD：无大面板；顶部显示关卡、分数、金币，崖口接近时出现窄幅“距断崖”风险条；连击只在发生变化时短暂强调。
- 远征队：覆盖层使用暖白考察日志底板，顶部固定当前金币与收藏进度；52 名角色以两列滚动卡片排列，卡片包含库内缩略图、名称、类别、抓地条、价格/已拥有/使用中，缩略图使用延迟加载。可滚动卡片使用 `onClick`，每个目标至少 92×156 px；滚动时不得触发购买或选择。
- 状态：焦点使用 3 px 暖雪白外框；禁用降至 42% 不透明度；警告同时用文字、姿态和红色；成功同时显示距离数字、评价文字和粒子。
- Emoji 规则：禁止用 emoji 作为功能图标或游戏角色素材。

## 8. 动效与特效

- 性格：短促、略带橡皮筋弹性；普通 UI 160–220 ms，关卡切换 420 ms。
- 首屏：角色近景持续轻微呼吸与左右重心转移；引导手指必须复用 Google Material Icons `touch_app` 标准路径，不允许重新手绘。图标以 1.5 秒周期完成“抬起—按下—指尖波纹扩散”，首个真实输入后立即消失；减少动态时手指保持静态并显示短标签。
- 开始：点击或换关后先用 520 ms 完成近景下蹲蓄力与伸展；角色实际开始移动的第一帧开放急停输入，并同步启动 1050 ms 拉远，再保持 1000 ms 全景建立镜头。第二关及之后不得从全景直接开始。
- 行进：镜头在前 15% 路程保持全景，之后推进、推近和绕行与危险进度连续绑定；三段速度变化分别表现为快速起步、中段冲刺和临崖持续高速。人体动作包含后外侧蹬冰、单脚滑行、双臂侧展与速度驱动的步频，动物使用对角足组、身体起伏与俯仰平衡；避免走路循环和全身统一摇摆。玩家在整段滑行运镜中随时可以点击急停。
- 随机巨物彩蛋：角色滑出后 520 ms 内随全景拉远从雾中淡入；动物使用 5.8 秒呼吸起伏，怪兽使用 6.4 秒侧摆，机甲与牛头怪使用更重的 7.2 秒俯仰。跑道进度达到 24% 后开始淡出，36% 时完全消失；连续两局不得出现同一角色。减少动态时取消起伏、侧摆和俯仰，只保留渐显渐隐。
- 输入：单击同帧锁定急停，角色按体型后仰 12–18°并压低；50 ms 内雪屑出现，操作提示立即切换为“制动中”，松手不改变状态。
- 收藏：购买成功后金币数字下跳、卡片印章由价格切换为“已拥有”，角色模型在 220 ms 内弹入当前选择；余额不足使用水平 4 px 轻摇并显示差额，不弹通用错误框。
- 停车：180 ms 定住，100 分制成绩在 140 ms 内向上弹入；0–220 px 的过关结算保持不限时，主按钮预告“下一位角色”。超过 220 px 时盖上“挑战无效”红色票据章，说明“你停在了安全观景区 / 未进入断崖警戒线”，以测距虚线贯穿人物到崖口，1450 ms 后才显示“本关重试”。贴边额外有冰崖暖光和 8 枚旋转的低多边形冰晶。
- 失败：140 ms 冻结建立冲击点，180 ms 内角色碎成 14 块，随后约 930 ms 慢速散落；镜头仅在碎裂瞬间轻震一次，位移不超过 4 px、时长 100 ms。
- 粒子：雪屑为 3–7 px 暖白椭圆；奖励冰晶为 4–8 px 黄白菱形，生命周期 220–480 ms。
- 减少动态：移除屏幕震动、背景风带和大量粒子；保留角色姿态、距离数字与评价文本。

## 9. 参考转译原则

- 《跳一跳》与《Block Hop》：采用待机小跳、四肢节奏、天气平滑轮换、金币收藏和即时重开；不复制道路、车辆、方块或原场景构图。
- 定点停车类街机：采用越接近危险边缘奖励越高的风险结构；不使用真实车辆仪表盘。
- 纸雕动画：采用色块分层和轻微材质划痕；不模拟复杂纸张噪声或厚重阴影。

## 10. 反模式

- 禁止 emoji、混合风格图标、写实冰川照片和平滑高模企鹅素材。
- 禁止显示旧版 Aigram / Alter 角落水印；游戏画面不叠加任何平台废弃品牌标记。
- 禁止玻璃拟态、霓虹光晕、大面积渐变、通用浮动白卡和无意义阴影。
- 禁止回到“单块白板”冰面；禁止让雪屑遮住冰崖边缘；禁止在首屏用大型按钮或卡片盖住角色；禁止固定全景镜头把角色留在远处；推进与旋转必须连续对应危险进度，不能使用随机抖动、景深虚化或突然缩放影响判距。
- 禁止用同一缩放掩盖角色体型差异，禁止让抓地数值与体型观感相反，禁止让商店覆盖层在游戏中误触打开。
- 禁止把 HUD 做得比角色更亮，禁止结算层遮蔽坠落原因。
- 禁止让随机巨物变成清晰、饱和或带发光描边的主角；禁止它遮挡跑道、崖口、角色和距离标尺，也禁止添加彩蛋提示文字、奖励或突发惊吓音效。禁止每次渲染重新抽取导致模型跳变，禁止连续两局重复同一彩蛋。
- 跑道下方禁止圆锥、冰锥、圆柱、尖刺和自然岩根造型；厚度变化必须由边缘清晰的长短方块表达。禁止一整面等深方墙，也禁止所有方块长度相同。

## 11. 垂直切片验收

- 入口：无大型开始按钮，角色在英雄机位中完整可见；幽灵手指与 2–4 字标签提示直接点击，且不覆盖角色面部、身体或脚部。“远征队”入口同时显示当前角色和永久金币。
- 游戏：至少 3 层冰壁、裂缝、冰柱和碎冰可辨；起步全景同时展示起点和崖口，镜头随危险进度从侧后方旋转推进到角色前侧近景，距断崖风险条不遮挡赛道。
- 收藏：两种体型差异显著的角色在同一机位中仍可一眼区分；购买、选择、余额不足和已拥有状态完整。
- 天气：晴朗、飘雪、薄雾、风雪四种状态均不遮蔽角色、崖口或操作提示。
- 高反馈：贴边停车同时具备后仰定格、距离数字、评价文字、暖光和冰晶粒子。
- 结束：先完整展示坠落原因，再出现包含本局分数和再来一次的结算层。
- 窄屏：320×568 下文字不截断、按钮不小于 44×44 px、HUD 与赛道不碰撞。
- 视觉 QA：第一轮 390×700 截图发现企鹅与冰崖被横向裁切，已通过统一 3D 坐标映射和镜头 zoom 修正；第一轮 320×568 截图发现 transform 缩放后右侧溢出，已改为视口中心绝对定位并复拍通过。证据保存在 `_qa/ui/`。
- 远征队改版 QA：首轮 390×700 发现雪帽外沿仍过直、素材人物侧面过薄、换关瞬间相机停留崖边；复验版已使用 5 段错位雪帽、角色三分之四朝向和结算期镜头回收修复。已覆盖企鹅/人物/动物、购买、晴/雪/雾/风雪、中文/英文及 320×568，证据保存在 `_qa/ui/expedition-v3/`。
- 长跑道改版 QA：首轮发现固定 zoom 30 的正式机位让人物持续偏小、人物朝向偏侧、320×568 首屏副标题与角色头部相碰；复验版把镜头改为 zoom 28 全景到 zoom 60 前侧近景的进度驱动长镜头，人物朝向改为 0.52rad、首屏观察点抬至 y=1.12，并把坠落提示移出人物特写区域。已复拍首屏、780 ms 拉远、全跑道、中段、临崖、坠落、人物滑行/急停及 320×568，证据保存在 `_qa/ui/runway-v4/`。
- 全景停留改版 QA：第一轮 320×568 发现 R3F canvas 在游戏场 transform 后再次按已缩放尺寸布局，造成右侧跑道裁切；修复为 390×700 固定逻辑画布并按实际渲染比例补偿正交相机 zoom。复验确认 780 ms 拉远后，从起点到尽头悬崖的整条跑道连续可见，并在两个仍为 `ready` 的采样帧之间保持锁定；随后才进入推进。人物/动物统一改为 Y=90° 朝 +X，企鹅滑行、急停和坠落近景已复拍。证据保存在 `_qa/ui/runway-v5/`。
- 肢体动作与长跑道 QA：390×700 下逐帧复拍人物左右蹬冰、急停外撑与坠崖；滑行连续帧可见左右肢体相位互换，急停帧可见双臂双脚展开。旧版完整模型翻滚因穿模已废弃；320×568 下的重试准备近景与全跑道镜头证据保存在 `_qa/ui/runway-v8-final/`，新碎裂慢动作及重试停留 2 秒的证据保存在 `_qa/ui/runway-v9-first/`。
- 全跑道语义修正版 QA：需求澄清为“看全整条跑道直到尽头”，不要求展示整条跑道阴影。第一轮复拍发现长尺寸接触阴影在海面形成矩形暗块，已移除该组件并保留角色实时阴影；390×700 与 320×568 复验均能从起点连续看到尽头悬崖，准备阶段状态仍为 `ready`，控制台无警告或错误。证据保存在 `_qa/ui/runway-v6/`。
- 逐关开局镜头修正版 QA：第一轮复验确认第 2 关过去会继承全景，现已把 780 ms 拉远从 ready 迁移到 playing；第 2 关实测 `ready / level=2 / x=40` 为角色近景，随后 `playing / level=2 / x>40` 才开始拉远。准备文字由 y=292 上移到 y=220，避免近景遮挡角色。第一关近景滑出、拉远、全跑道停留及第二关近景重启证据保存在 `_qa/ui/runway-v7/`。
- 成败镜头语言 QA：390×700 逐帧复拍成功定格、环绕、英雄特写与失败冻结、碎裂、垂直下坠；成功稳定展示角色正面、分数、冰晶和崖口，失败保持崖壁为垂直参照且无横向怪转。320×568 首轮发现成功距离文字接近角色头部，已将成功字幕从 y=118 上移到 y=96、主分数从 66px 收至 62px，并以相同尺寸复拍成功与失败通过。证据保存在 `_qa/ui/outcome-cameras-v1/`。
- 过早停车失败 QA：390×700 与 320×568 均复拍定格、升高拉远、完整剩余跑道和低分结算；镜头清楚保留人物与崖口两个端点，红色票据章与珊瑚红测距虚线说明“未进入断崖警戒线”，且不会自动重开。证据保存在 `_qa/ui/outcome-cameras-v2/`。
- 远景巨物彩蛋 QA：第一轮巨物在全景中尺寸与对比不足，放大后又暴露出水平海面网格的矩形边界，视觉上误读为跑道下方阴影；继续扩大网格后，正交相机近端裁切仍留下水平边界。最终移除水平海面网格，改用无边界底色、浪脊和碎冰表达深海；11 类候选归一为 28–34u 高、放置在 z=-15 至 -18，并用固定高度裁切仅显示顶部约 40%。390×700 与 320×568 全景均不再出现矩形暗块或水平边界，巨物只露出局部且不遮挡跑道、崖口、角色或 HUD。证据保存在 `_qa/ui/distant-easter-eggs-v1/`。
- 浮岛跑道 QA：锥形首版与用户要求的方块语言不符，已废弃。390×700 与 320×568 方块复验确认顶部跑道连续，两段长方体伸出画框，两段中短方体与 6 个短方块在画面内结束，块间保留负空间，最后崖口仍为薄悬挑；没有圆锥、圆柱、贯穿全长的方墙或海面暗带。匹配对比证据为锥形版 `25/26-floating-island` 与最终方块版 `27-block-island-final-390x700.png`、`28-block-island-final-320x568.png`，均保存在 `_qa/ui/distant-easter-eggs-v1/`。
- 水印统一为 `components/Watermark.tsx` 引用的单色白色 `img/alteru.svg`（SHA-1 `f4f1d04b29bd2a14493adf66ff3304eab30ba11c`）；右下角 32×32px、透明度 0.28，不使用旧完整字标、暖色版、黑色版或任何旧文件名。390×700 与 320×568 的首轮和同状态复验截图保存在 `_qa/ui/watermark-f4f1d0/`，均未遮挡 HUD、跑道或操作提示。

## 12. 屏幕与状态合同

- 开始：玩家需要理解当前角色的抓地力、“会向右滑、点一下便锁定急停”；焦点是角色与断崖，主动作是“开始滑行”，次动作是“远征队”。
- 准备：520 ms 角色近景起步倒计时，以角色下蹲和向前伸展表示即将启动；准备阶段点击不会锁定急停。滑行阶段开始后才执行 1050 ms 拉远与 1000 ms 全景锁定，并立即接受急停。
- 滑行：顶部仅保留关卡、分数、金币；全画面单击一次锁定急停，底部从“点一下急停”切换到不可逆的“制动中”。
- 停车结算：显示单关成绩、距离、金币、是否过关和下一位角色缩略图；不限时停留，必须由玩家点击“下一位角色”或“本关重试”，绝不自动开始。低分点击“本关重试”后仍停在角色起点近景与标准幽灵手指提示，直到玩家再次点击场景。
- 坠落：隐藏操作提示，保留 HUD，1250 ms 慢动作碎裂散落完成后出现结算层。
- 远征队：显示永久金币、收藏进度、全部 52 名角色、类别、抓地条、价格和选择状态；关闭后回到打开前的封面或结算页。
- 游戏结束：显示本局分数、通过关数、本局金币与永久金币，主动作“再来一次”，次动作“远征队”和“返回封面”。
- 声音按钮固定右上角 48×48 px；无加载、联网、权限与社交状态。

## 13. 手感反馈矩阵

| 事件 | 即时反馈 | 结果与通道 | 强度 | 恢复 |
|---|---|---|---:|---|
| 开始 | 幽灵手指消失、角色近景下蹲并滑出 | 滑动同帧开放急停并开始拉远；1050 ms 到全景、停留 1000 ms | 1 | 全景停留后进入推进 |
| 单击急停 | 同帧后仰、身体压低、双脚外撑、双臂甩向身后 | 雪屑、双手反相晃动、刹车下降音、长距离自动减速 | 3 | 等待停车或坠落，不可撤销 |
| 安全停车 | 120 ms 定格后绕到角色正前方，显示 45–75 分 | 青绿光环、冰晶爆发与单音 | 3 | 1550 ms 后显示结算 |
| 漂亮停车 | 定格后快速绕正并推至 zoom 78，显示 90 分 | 金白光环、较密冰晶与双音 | 4 | 1550 ms 后显示结算 |
| 贴边停车 | 定格后快速绕正并推至 zoom 78，显示 100 分 | 暖光、20 枚金色冰晶、扩散光环、三音上行与连击增长 | 5 | 1550 ms 后显示结算 |
| 停得太远 | 110 ms 定格后升高拉远至 zoom 7.4，完整展示剩余跑道 | 珊瑚红测距虚线、红色 X、挑战无效票据章与两段下行音 | 4 | 1450 ms 后显示低分结算 |
| 坠落 | 140 ms 冻结，不横向绕人 | 镜头沿崖壁下坠追随碎片、zoom 84→62、下降音与失败文字 | 5 | 1250 ms 后事故结算 |
| 下一位角色 / 本关重试 | 按钮下压、短音 | 自动换角并进下一关，或保留角色重试 | 1 | 520 ms 近景准备后开局 |
| 获得金币 | 金币数字上跳、金色粒子 | 150 ms 双音，永久余额更新 | 2 | 继续下一关 |
| 解锁角色 | 卡片印章翻转、角色弹入 | 三音上行、立即选中 | 4 | 关闭商店或继续浏览 |
| 余额不足 | 金币差额文字、卡片轻摇 | 90 Hz 短促闷音 | 2 | 保留商店位置 |

- 输入到视觉反馈目标小于 16 ms；输入到声音反馈目标小于 50 ms。
- 静音时全部关键反馈仍由姿态、文字和粒子表达。
- 减少动态时取消震动与背景循环，只保留不超过 220 ms 的必要状态过渡。
